# Shopify Ruby styling (Telos standard); the gem bundled in the Gemfile
inherit_gem: { rubocop-shopify: rubocop.yml }

# Overwrite or add rules to create your own house style
#
# # Use `[a, [b, c]]` not `[ a, [ b, c ] ]`
# Layout/SpaceInsideArrayLiteralBrackets:
#   Enabled: false
